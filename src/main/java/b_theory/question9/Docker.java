package b_theory.question9;

public class Docker {

    //todo A
    // What is a server?
    // Answer: Server is basically a PC which can provide different functionalities to user.

    //todo B
    // What is the difference between build server and production server?
    // Answer: The difference is that production server is used to host a code and build server to continuously build code

    //todo C
    // What is docker?
    // Answer: Its a tool that runs applications using containers

    //todo D
    // Name and explain docker container benefits over virtual machine setup (java jar as system process and installed nginx)
    // 1 Docker is lighter in terms of resources. Because docker containers share OSs.
    // 2 Docker starts up faster.

    //todo E
    // Name and explain docker container drawback over virtual machine setup (java jar as system process and installed nginx)
    // 1 It is harder to work with Docker, VMs tools are easier to access and simpler to work with.

    //todo F
    // Name and describe tools for docker architecture
    // 1 Gradle. Plugin that simplifies the script-building process.
    // 2 Gitlab CI. Automates the source code management.

    //todo G
    // Name and explain why are companies slow in moving existing systems to docker architecture (do not explain why docker is bad)
    // 1 Because companies usually want their apps to be secure.
    // 2 Probably because it is very expensive to move to docker. And people who can work with containers probably get completely different salary.
}
