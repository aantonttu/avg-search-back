package c_theory.question12;

public class Security {

    //todo A
    // What is authentication?
    // Answer: This is the process when submitted by user credentials are compared with those, which are saved in database in order to recognize user.

    //todo B
    // What is authorization?
    // Answer: Mechanism to give user privileges dependant on his role.

    //todo C
    // For web (HTTP backend) Spring Security is configured as a ... ? Explain it.
    // Think or read how Spring Security checks are injection into your application.
    // Configured as (one word): filter
    // Description: filters are used for creating/removing restrictions for unauthorized or authorized users with not enough privileges.

}
